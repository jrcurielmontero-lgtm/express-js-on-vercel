   "version": 2,
   "builds": [
         {
            "src": "api/sendToBrevo.js",
            "use": "@vercel/node"
         }
    ],
    "installCommand": "pnpm install --no-frozen-lockfile",
    "routes": [
        { "src": "/(.*)", "dest": "api/sendToBrevo.js" }
    ]
}