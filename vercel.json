{
   "version": 2,
   "builds": [
         {
            "src": "server.js",
            "use": "@vercel/node"
         }
    ],
    "installCommand": "pnpm install --no-frozen-lockfile",
    "routes": [
        { "src":"/(.*", "dest": "sendToBrevo.js" }
    ]
}